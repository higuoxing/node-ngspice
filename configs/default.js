module.exports = {
  // default configuration
  deploy_domain: 'http://localhost',      // deploy index
  port: 3000,                             // given port
  tmp_file_path: '/tmp/',                  // tmp file path
}
